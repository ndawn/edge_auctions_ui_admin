import items from './items';
import auctions from './auctions';
import components from './components';
import other from './other';

const menuItems = {
    items: [items, auctions, components, other]
};

export default menuItems;
